#ifndef MATH_H_INCLUDED
#define MATH_H_INCLUDED


struct Retangulo
{
    float b;
    float h;
};

float area(float);

float area(float,float);

float area(Retangulo r);

int power(int base, int pow=0);


#endif // MATH_H_INCLUDED
